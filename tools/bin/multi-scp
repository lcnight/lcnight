#! /bin/bash

usage()
{
    echo "`basename $0` src-files dst"
    exit
}
if [[ $# < 2 ]]; then
    usage
fi

list=`cat /etc/hosts | grep 192.168 | cut -f2`
for i in $list; do
    docmd="scp -q $1 $i:$2"
    echo "running: $docmd"
    eval $docmd
    echo
done
# sshlogin $1 "$2"
